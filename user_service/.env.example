# ==================================
# User Service - Environment Configuration
# ==================================
#
# Copy this file to .env and configure with your values:
# cp .env.example .env
#

# ==================================
# PostgreSQL Database Configuration
# ==================================
POSTGRES_DB=electic_shop_db
POSTGRES_USER=electic_user
POSTGRES_PASSWORD=electic_password
POSTGRES_HOST=localhost
POSTGRES_PORT=5433

# ==================================
# Redis Cache Configuration
# ==================================
REDIS_HOST=localhost
REDIS_PORT=6379

# ==================================
# JWT Configuration
# ==================================
# Secret key for JWT signing (min 256 bits / 32 characters)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-secret-key-change-this-in-production-min-256-bits

# Access token expiration (in milliseconds)
# 3600000 = 1 hour
JWT_EXPIRATION=3600000

# Refresh token expiration (in milliseconds)
# 604800000 = 7 days
JWT_REFRESH_EXPIRATION=604800000

# ==================================
# Security Configuration
# ==================================
# HMAC secret for password reset and email verification tokens
# Generate with: openssl rand -base64 32
HMAC_SECRET=your-hmac-secret-change-this-in-production

# ==================================
# Email Configuration
# ==================================
# Email address used as sender
MAIL_FROM=noreply@electricshop.com

# Password Reset Email Settings
PASSWORD_RESET_SUBJECT=Password Reset Request - Electric Shop
PASSWORD_RESET_EXPIRY_MINUTES=15

# Email Verification Settings
EMAIL_VERIFICATION_SUBJECT=Verify Your Email - Electric Shop
EMAIL_VERIFICATION_EXPIRY_MINUTES=1440

# ==================================
# Application Configuration
# ==================================
# Frontend application URL (used in email links)
APP_BASE_URL=http://localhost:3000

# Spring profile (dev, prod, test)
SPRING_PROFILES_ACTIVE=dev

# ==================================
# Notes
# ==================================
# - Keep this file in version control
# - NEVER commit the actual .env file (it should be in .gitignore)
# - Update values in .env file for your local/production environment
# - For production, use strong randomly generated secrets
